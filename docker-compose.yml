services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_DB: hub_metrics
      POSTGRES_USER: hub
      POSTGRES_PASSWORD: hub
    ports:
      - "5432:5432"
    volumes:
      - hub_metrics_data:/var/lib/postgresql/data

volumes:
  hub_metrics_data:
